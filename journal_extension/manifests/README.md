# Manifests

No restricted V1 manifest is committed here.

At execution time, provide the canonical frozen manifest and class map from restricted storage.
The JE core verifies their SHA-256 identities before any row is resolved. Public Git records only
their expected identities and run-time verification results.
