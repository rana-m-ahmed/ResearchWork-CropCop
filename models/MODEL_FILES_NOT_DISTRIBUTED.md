# Model files are not distributed

Checkpoints, converted graphs, raw logits, and the PTE binary are excluded pending upstream-licence and redistribution review. This repository publishes exact hashes, sizes, model cards, aggregate/per-class metrics, runtime parity evidence, and integration contracts.

Do not substitute an object with the same filename: metrics apply only to the exact hashes in `model_state_registry.json`.
