# Metrics

`metric_registry.json` is the public headline registry for the bootstrap repository. `main_results.csv`, `diagnostic_probes.csv`, and `ptq_candidates.csv` preserve the state-separated results used by the manuscript. Complete 120-class tables remain in the validated handoff package and will be attached to the frozen preprint release.
